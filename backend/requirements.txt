# Backend (dockerless, local-first)
#
# This backend is optimized for personal/local use:
# - SQLite instead of Postgres/PostGIS
# - Earth Engine used for server-side compute + tiles (no GDAL/rasterio dependency)

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Storage
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# HTTP + exports
httpx>=0.26.0
pillow>=10.2.0
imageio>=2.33.0
reportlab>=4.0.0

# Satellite compute
earthengine-api>=0.1.380

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
